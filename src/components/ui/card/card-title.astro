---
import { cn } from "@/lib/utils";

interface Props {
  class: string;
}

const { class: className } = Astro.props;
---

<div class={cn("font-semibold leading-none tracking-tight", className)}>
  <slot />
</div>
