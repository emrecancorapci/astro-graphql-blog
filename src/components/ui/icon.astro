---
import type { IconName } from '@/lib/lucide';

interface Props {
  icon: IconName;
  class?: string;
}

// https://lucide.dev/icons/

const { icon, class: className } = Astro.props;
---

<i class={className} data-lucide={icon}></i>
