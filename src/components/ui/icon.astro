---
import type { IconName } from '@/lib/lucide';
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<"i"> {
  icon: IconName;
}

const { icon, ...props } = Astro.props;
---

<i data-lucide={icon} {...props}></i>
